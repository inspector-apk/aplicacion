.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
android/
ios/
linux/
macos/
windows/
web/
*.iml
*.class
.idea/

# Mantenemos el APK ya compilado versionado; solo ignoramos el resto
# de artefactos de build de Flutter si algún día existieran aquí.
build/*
!build/Inspector.apk

# Backend: nunca versionar secretos, dependencias ni la base de datos local.
backend/.env
backend/node_modules/
backend/*.db
backend/*.db-shm
backend/*.db-wal

